package ${packages}.dao.spi;

import com.vteba.tx.jdbc.spring.spi.SpringGenericDao;
import ${packages}.model.${className};

/**
 * ${tableName}Spring Dao接口。
 * @author yinlei
 * date ${currentDate}
 */
public interface ${className}SpringDao extends SpringGenericDao<${className}, ${pk}> {

}
